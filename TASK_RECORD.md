# Task Record

## 2026-06-05 首页分类与商品加载链路

- 首页先加载分类列表，分类导航栏保持稳定。
- 点击分类时只根据 `category_id` 重新请求商品列表，不重新渲染整个页面。
- 分类切换期间只让商品区域进入加载态，左侧分类导航栏不再被骨架屏替换。
- 商品接口保持分页请求方式，默认每页 12 条，继续支持下拉加载下一页。
- 未采用商品聚合查询方案，避免改变当前接口查询语义。

## 2026-06-05 数据库索引链路

- 新增商品查询索引，优化首页全部商品与按分类商品分页。
- 新增分类查询索引，优化前台分类列表与后台分类管理。
- 新增卡密查询索引，优化库存统计、订单卡密读取和商品卡密维护。
- 新增订单查询索引，优化联系方式查单、用户订单、状态筛选、支付回调和后台统计链路。
- 索引迁移做了幂等处理，避免服务器上已手动创建过索引时重复执行失败。

## 2026-06-05 商品分类缓存链路

- Redis 使用全局连接池与全局客户端，避免每次请求创建新连接。
- 首页商品接口按分类缓存完整商品数组，缓存 key 包含 `all` 与具体 `category_id`。
- 商品分页请求直接从分类缓存中切片返回，不再为每个 `offset/limit` 生成碎片缓存。
- 商品搜索在分类缓存结果上过滤，再执行分页切片。
- 商品、分类、卡密变更时统一清理分类商品缓存，保证库存、已售与上下架状态更新后可刷新。

## 2026-06-06 商品接口快慢波动排查

- 后端增加分类商品缓存重建单飞锁，避免多个未命中分类并发触发重复全量重建。
- 分类商品缓存构建时预热所有有效分类，包括暂无商品的空分类，避免空分类反复回源数据库。
- 前端切换分类时取消上一轮商品请求，避免快速点击分类时旧请求继续占用连接并造成瀑布流抖动。
- 开发环境 React StrictMode 会让初始化 effect 试运行两次，商品请求 cleanup 会中止上一轮请求，降低本地调试时的重复请求影响。
- 分类商品缓存从多 key 改为单个 Redis Hash，field 为 `all` 或分类 id，减少重复数据写入和 key 管理成本。
- 缓存构建查询改为只读取商品展示所需字段，避免 ORM `selectin` 关系额外加载分类、卡密、订单等数据。
- Vite 开发代理改为固定 `127.0.0.1` 后端地址，并启用 HTTP keep-alive agent，减少开发环境反复建立代理连接造成的“初始连接”耗时。

## 2026-06-06 前端首屏包体优化

- 前台、后台、登录、支付、订单查询、商品详情等页面改为路由级懒加载。
- 首页首屏不再一次性下载后台管理和其他低频页面代码。
- 删除未使用的 `useHomeData` hook，减少维护噪音。
- 构建后主入口 JS 从约 598KB 降至约 261KB，首页页面代码独立为约 25KB chunk。
