# 我的订单慢请求检测记录

检测时间：2026-06-06 22:26-22:30  
检测账号：sanye（密码不写入文档）  
接口：`GET /api/v1/orders/mine?contact_info=3194584108%40qq.com&after_id=0&limit=10`

## 一、复现结果

当前 8000 端口运行的旧后端请求耗时：

| 项目 | 结果 |
| --- | ---: |
| 登录接口总耗时 | 475.47ms |
| 我的订单接口总耗时 | 6920.26ms |
| 返回记录数 | 26 条 |

结论：截图中的 6-7 秒可以稳定复现。当前 8000 服务仍在运行旧订单列表逻辑，一次性返回全部订单，并且日志中出现大量重复 SQL。

## 二、新增耗时记录点

已在后端加入两类诊断日志，搜索关键字即可看到：

```text
[auth.timing]
[orders.mine.timing]
```

### 1. 认证耗时

文件：`backend/app/api/deps.py`

| 字段 | 含义 |
| --- | --- |
| `jwt_decode` | JWT 解码耗时 |
| `redis_token_get` | Redis 读取登录 token 耗时 |
| `total` | 认证依赖总耗时 |

本次 8001 诊断服务实测：

| 阶段 | 耗时 |
| --- | ---: |
| JWT 解码 | 0.13ms |
| Redis token 获取 | 38.95ms |
| 认证依赖总计 | 39.09ms |

### 2. 我的订单后端耗时

文件：`backend/app/api/v1/orders.py`

| 字段 | 含义 |
| --- | --- |
| `auth_dependency_done` | 进入订单处理函数后的起点 |
| `db_connection_checkout` | 获取数据库连接耗时 |
| `query_order_ids` | 查询当前页订单 id 耗时 |
| `query_order_rows` | 查询订单展示字段耗时 |
| `serialize_records` | Python 组装返回记录耗时 |
| `query_total_count` | 查询订单总数耗时 |
| `query_has_more` | 判断是否还有下一页耗时 |
| `build_response` | 后端处理完数据、即将返回前的组装耗时 |

本次 8001 诊断服务实测：

| 阶段 | 单段耗时 | 累计耗时 |
| --- | ---: | ---: |
| 获取数据库连接 | 557.37ms | 557.37ms |
| 查询订单 id | 53.10ms | 610.47ms |
| 查询订单展示字段 | 46.50ms | 656.97ms |
| 序列化返回记录 | 0.10ms | 657.06ms |
| 查询总数 | 46.93ms | 703.99ms |
| 判断是否还有下一页 | 295.33ms | 999.33ms |
| 组装响应 | 0.00ms | 999.33ms |

外层接口总耗时：1044.75ms，返回 10 条，总数 23，有下一页。

## 三、慢点判断

### 主要慢因：旧接口存在 N+1 查询

旧 8000 服务日志显示，请求期间不断重复执行：

```text
SELECT code_key ... WHERE code_key.order_id = ? AND code_key.status = ?
SELECT order ... WHERE order.id IN (?)
SELECT product ... WHERE product.id IN (?)
SELECT user ... WHERE user.id IN (?)
SELECT category ... WHERE category.id IN (?)
```

这些 SQL 在 22:26:45 到 22:26:51 期间连续出现，和 6.92 秒请求耗时吻合。订单越多，请求越慢。

### 次要慢因：数据库连接/网络本身偏慢

新版接口已经避免一次性返回全部订单和卡密循环查询，但本次仍看到：

| 项目 | 耗时 |
| --- | ---: |
| 数据库连接获取 | 557.37ms |
| `has_more` 查询 | 295.33ms |
| Redis token 获取 | 38.95ms |

另外 `backend-local.err.log` 中出现过 MySQL 断连：

```text
Lost connection to MySQL server during query ([WinError 121] 信号灯超时时间已到)
```

说明数据库连接链路有抖动，需要继续观察。

## 四、已做修改

1. `/orders/mine` 改为认证用户专用接口，不再用游客查询逻辑兜底。
2. 首屏只查询 10 条订单 id，再查询展示字段。
3. 列表接口不返回卡密，避免每条订单额外查 `code_key`。
4. 增加游标分页字段：`prev_cursor`、`next_cursor`、`has_more`。
5. 增加诊断日志 `[auth.timing]` 和 `[orders.mine.timing]`。

## 五、建议继续优化

1. 确认生产/本地 8000 服务已重启并加载新代码，否则仍会使用旧的慢逻辑。
2. 确认数据库已经执行索引迁移，尤其是 `ix_order_user_cursor(user_id, id, created_at)`。
3. 后续可把 `has_more` 查询改成多取 1 条，避免额外 `EXISTS` 查询。
4. 检查 MySQL 与应用是否在同机或同内网，当前数据库连接获取 557ms 偏高。
5. 如果 `DEBUG=True`，建议生产关闭 SQL echo，否则大量 SQL 日志本身也会拖慢接口。

## 六、验证命令

```powershell
$env:PYTHONPATH='.'
pytest tests\test_order_payment_flow.py::test_my_orders_uses_cursor_pagination tests\test_auth_flow.py::test_admin_login_refresh_me_and_logout
```

结果：2 passed。

## 七、二次优化记录

2026-06-06 后续已继续优化 `/orders/mine`：

1. 删除订单列表接口中的 `count(*)` 总数查询。
2. 删除额外 `exists()` 查询，改为一次查询 `limit + 1` 条来判断 `has_more`。
3. 删除先查 id 再查订单行的两段式查询，改为一次查询订单展示字段。
4. 游标升级为复合游标：`after_created_at + after_id`。
5. 前端不再显示 `已加载 x / total`，改为 `已加载 x 条购买记录`。
6. 排查日志 `[auth.timing]` 和 `[orders.mine.timing]` 已从 warning 降为 info。

本地真实账号验证：

| 阶段 | 耗时 | 返回 |
| --- | ---: | --- |
| 第一页 | 343.88ms | 10 条，`has_more=True` |
| 第二页 | 155.10ms | 10 条，`has_more=True` |

第一页返回不再包含 `total` 字段。
